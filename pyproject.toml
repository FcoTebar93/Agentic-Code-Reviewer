[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = [
    "e2e: contra Gateway real con Docker Compose; usar ADMADC_E2E=1",
    "e2e_security_block: E2E negativo security.blocked (ver test_security_blocked_e2e)",
    "e2e_qa_fail: E2E negativo qa.failed (ver test_qa_failed_e2e)",
    "integration: integración sin stack completo (carpeta tests/integration)",
]

[tool.ruff]
line-length = 88
target-version = "py312"
exclude = [
  ".git",
  ".venv",
  "venv",
  "__pycache__",
  "node_modules",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"services/gateway_service/routes/*.py" = ["B008"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_unused_configs = true
check_untyped_defs = false
exclude = "(^frontend/|^infrastructure/)"
